import React from "react";

import { TWLoadingIcon } from "./ButtonStyle";

function LoadingIcon({ className, ...rest }) {
  return <TWLoadingIcon className="tw-loading-1" {...rest} />;
}

export default LoadingIcon;
